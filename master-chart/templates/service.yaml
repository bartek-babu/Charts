apiVersion: v1
kind: Service
metadata:
  name: {{ include "master-chart.fullname" . }}
  labels:
    {{- include "master-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "master-chart.selectorLabels" . | nindent 4 }}
